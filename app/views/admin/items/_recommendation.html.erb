<% unless item.is_recommended %>
  <%= link_to t(:recommended), change_recommendation_admin_items_path(:item_id => item.id), :remote => true %>
<% else %>
  <%= link_to t(:non_recommended), change_recommendation_admin_items_path(:item_id => item.id), :remote => true %>
<% end %>
